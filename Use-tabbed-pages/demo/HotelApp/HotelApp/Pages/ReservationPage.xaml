<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="HotelApp.Pages.ReservationPage"
             BackgroundColor="{DynamicResource SecondaryColor}"
             Title="Reserve Room">

    <Grid RowSpacing="2">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>

        <Label Grid.Column="0" Grid.Row="0" Text="Credit Card number:" />
        <Editor Grid.Column="1" Grid.Row="0" WidthRequest="200"/>

        <Button Grid.Column="0" Grid.Row="1" Text="Reserve Room" Clicked="OnFinish"/>
    </Grid>
</ContentPage>
